{% extends 'base.html.twig' %}

{% block title %}Ajouter un coordinateur — {{ classe.name }}{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/admin.css') }}">
    <link rel="stylesheet" href="{{ asset('styles/rooms.css') }}">
    <link rel="stylesheet" href="{{ asset('styles/classes.css') }}">
{% endblock %}

{% block body %}
<div class="container-fluid px-4 py-3 mt-3">

    {# ---- Header ---- #}
    <div class="dashboard-header">
        <div class="d-flex align-items-center justify-content-between">
            <div>
                <div class="d-flex align-items-center gap-2 mb-2">
                    <span class="badge-admin">Administrateur</span>
                </div>
                <div class="d-flex align-items-center gap-2 mb-1">
                    <a href="{{ path('app_admin_dashboard') }}" class="breadcrumb-link">Dashboard</a>
                    <span class="breadcrumb-sep">›</span>

                    <a href="{{ path('app_admin_classes') }}" class="breadcrumb-link">Classes</a>
                    <span class="breadcrumb-sep">›</span>

                    <a href="{{ path('app_admin_classe_show', {id: classe.id}) }}" class="breadcrumb-link">{{ classe.name }}</a>
                    <span class="breadcrumb-sep">›</span>

                    <span class="breadcrumb-current">Ajouter un coordinateur</span>
                </div>
                <h1>Ajouter un coordinateur à {{ classe.name }}</h1>
                <p>Sélectionnez un coordinateur existant à assigner à cette classe</p>
            </div>
        </div>
    </div>

    {# ---- Formulaire ---- #}
    <div class="row justify-content-center ">
        <div class="col-lg-5 mt-4">
            <div class="form-card shadow">

                {{ form_start(form) }}

                    <div class="form-field">
                        {{ form_label(form.coordinator) }}
                        {{ form_widget(form.coordinator) }}
                        {{ form_errors(form.coordinator) }}
                    </div>

                    <div class="d-flex gap-2 mt-4">
                        <button type="submit" class="btn-action-primary flex-fill text-center">
                            ✓ Ajouter à la classe
                        </button>
                        <a href="{{ path('app_admin_classe_show', {id: classe.id}) }}"
                           class="btn-action-secondary flex-fill text-center text-decoration-none">
                            Annuler
                        </a>
                    </div>

                {{ form_end(form) }}

            </div>

            <div class="form-note mt-5">
                Seuls les coordinateurs n'appartenant pas encore à cette classe sont affichés.
                Pour créer un nouveau coordinateur,
                <a href="{{ path('app_admin_coordinator_new') }}" class="note-link">cliquez ici</a>.
            </div>
        </div>
    </div>

</div>
{% endblock %}
